name = "EventTest"

-- Every field accepts the singular string or the plural table form.
shared_scripts = { "shared.lua" }
client_scripts = { "client.lua" }
server_scripts = { "server.lua" }

ui_page = "ui/index.html"
files = {
    "ui/index.html",
}

name = "InputTest"

-- Control natives: IsControlPressed / IsControlJustPressed /
-- IsControlJustReleased / GetControlValue / IsPadConnected.
--
--   inputwatch [s]   log every press and release for a few seconds
--   holdtest         the "hold a direction at a marker" pattern
client_scripts = { "client.lua" }
